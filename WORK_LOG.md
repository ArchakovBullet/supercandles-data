<!-- VERSION: 2026-09-19 21:03 MSK | COMMIT: HEAD -->

## 15.09.2026 (ночная сессия — большая)

### ✅ Выполнено

**🎯 ЭКСПИРАЦИЯ (СРОЧНО):**
- Обнаружено: 9 позиций с экспирацией 17.09 (завтра!), 1 — 18.09
- Добавлена колонка `expiry_date` в `futures_positions`
- Заполнены через MOEX ISS (`LASTTRADEDATE`)
- Добавлена функция `check_expiry()` в `futures_robot.py`
- Закрыто **9 позиций** по EXPIRY:
  - **RI: +325 594.09₽**
  - RN: +2 563.00₽
  - TT: +2 128.00₽
  - SP: +485.00₽
  - MG: +103.00₽
  - Eu: +34.00₽
  - X5: +30.50₽
  - IB: +0.01₽
  - ME: −99.00₽
  - **Итого: +330 838.59₽**

**🐛 ИСПРАВЛЕНИЕ БАГОВ:**
- `point_value` в БД (для корректного PnL при изменении справочника)
- RI PnL = 325 594.09₽ — корректно (по текущему `point_value` = 168.7016)

**🎉 HI2 — ВОССТАНОВЛЕН:**
- Найдена причина: `engine_map` УЖЕ правильный (`stock→eq`, `futures→fo`)
- Реальная причина: **keyring corrupted** (`token0` duplicate)
- **Главная причина:** `hi2_collector.py __main__` неправильно загружал token:
  БЫЛО: `MOEXPy(token=os.getenv('MOEX_TOKEN') or load_dotenv(...) or os.getenv('MOEX_TOKEN'))`
  `load_dotenv` возвращает `True`, не token!
- **Патч:** правильная загрузка `.env` ДО `os.getenv`
- **Результат:** 297 записей на тикер, данные до 14.09.2026
- **`hi2_daily.parquet`:** 5415 строк (+193)

**🔄 ОТКЛЮЧЕНИЕ ПАР:**
- LKOH-HYDR_H4, LKOH-ROSN_H4, LKOH-TATN_H4, ROSN-TATN_H4, LKOH-IRAO_H4, HYDR-IRAO_H4
- Причина: `LKOH_H4.parquet` не обновлялся 28 дней (SuperCandles H4 — только для фьючерсов)

**📊 ПАТЧИ is_tf_fresh / is_futoi_fresh:**
- Универсальный поиск колонки с датой (`begin`, `tradedate`+`block`, `tradedate`+`tradetime`)
- Логирование ошибок (вместо `except: pass`)
- Проверка обеих ног пары (было только `file_a`)

**📋 Новые эндпоинты AlgoPack (от 20.08):**
- `/eq/hi2/` — акции ✅
- `/fo/hi2/` — фьючерсы ✅
- `/fq/hi2/` — валюта (404 — не работает)

### 📝 Заметки
- **Keyring:** `/root/.local/share/python_keyring/keyring_pass.cfg` — переименован в `.bak`
- **HI2 метрики (6 шт):** `hhi_agressive`, `hhi_volume`, `hhi_buy`, `hhi_agressive_buy`, `hhi_agressive_sell`, `hhi_netflow_buy`
- **TradeStats:** не используется в роботах (32 тикера, `disb` ∈ [−1, +1])
- **Правило:** НЕ использовать `load_dotenv()` внутри `or` — возвращает `True`

### 🎯 На следующий раз
- [ ] Интеграция TradeStats в `unified_scanner` (поле `disb`)
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2
- [ ] Бэктест стопов (`backtest_stop_levels.py`)
- [ ] Индивидуальный ATR по тикерам (CE, RB, MG — волатильные)
- [ ] Проверить дашборд — 🟢 HI2
- [ ] Разобраться с `fq/hi2/` (404) — запросить AlgoPack


## 15.09.2026 (утренняя сессия — PnL в пунктах + VK-fix)

### ✅ Выполнено

**🎯 PnL В ПУНКТАХ:**
- Идея: показывать PnL **в рублях + пунктах** (двойная метрика)
- **Зачем:** пункты — универсальная метрика, не зависит от `point_value`
- **Пример:** RI = `+325 594.09₽ (+1 930 пунктов)` — реальное движение vs эффект `point_value`

**Миграция БД:**
- `futures_positions`: добавлены `pnl_points REAL`, `point_value REAL`
- `positions` (парный): добавлены `leg_a_pnl_points`, `leg_b_pnl_points`, `total_pnl_points`

**Backfill (фьючерсный):**
- 28 тикеров получили `point_value` из `contract_points.json`
- Все закрытые сделки: `pnl_points = (exit − entry) × volume` (с учётом direction)
- **RI:** `+325594.09₽ (+1930 пунктов)` ✅

**Backfill (парный) — 2 попытки:**
- **Попытка 1 (гибрид):** прямой расчёт + обратный — **знаки перевёрнуты!**
- **Попытка 2 (только через `pnl / pv`):** корректно ✅
- **Формула:** `leg_X_pnl_points = leg_X_pnl / point_value_X`
- **Результат:** 17/48 сделок с `pts` (остальные — legacy с `exit_price = 0`)

**Статистика PnL:**
| Робот | PnL (₽) | PnL (pts) |
|-------|---------|-----------|
| Фьючерсный (всего) | +329 202 | +4 778 |
| — EXPIRY (9) | +330 838.59 | +7 175.15 |
| — EXPIRY_MANUAL (2) | +186.88 | +221.80 |
| — SIGNAL (2) | +128.02 | +130.22 |
| — STOP (29) | −1 951.26 | −2 748.60 |
| Парный (48 закрытых) | +152.13 | +146.70 |

**🐛 VK-ТОКЕН — ИСПРАВЛЕН:**
- **Проблема:** `check_contract_changes.py` — ошибка `[15] Access denied: token required`
- **Причина:** `load_dotenv` не было в скрипте → `VK_TOKEN = ""` → 401
- **Патч:** `load_dotenv('/root/finlab/.env')` до `os.getenv`
- **Тест:** сообщение в VK доставлено ✅

**📁 .gitignore — tf_fresh_state.json:**
- Файл меняется каждые 10 минут (служебный)
- Добавлен в `.gitignore`
- Убран из индекса (`git rm --cached`)

**Git:**
- Коммит `bf2d7a3` — PnL в пунктах + VK-токен fix
- Коммит `bca40d3` — gitignore: tf_fresh_state.json
- Оба запушены в `origin/master`

### 📝 Заметки
- **Схема `positions`:** `entry_price_a`, `exit_price_a`, `entry_price_b`, `exit_price_b` (не `leg_a_price`!)
- **`point_value` в БД:** фиксируется на момент **закрытия** (не пересчитывается)
- **Парный backfill:** только через `pnl / pv` (согласованно с рублёвым PnL)
- **`vk_api.VkApi`:** НЕ имеет `.messages`; правильный объект — `vk_session.get_api()`
- **Правило:** `load_dotenv` — ВСЕГДА явно в cron-скриптах

### 🎯 На следующий раз
- [ ] **Патч `close_position`** — считать `pnl_points` в коде (для будущих сделок)
- [ ] **Дашборд** — колонка `PnL (пункты)` (формат `+325K₽ (+1930 pts)`)
- [ ] **VK-уведомления** — формат `+325594.09₽ (+1930 пунктов)`
- [ ] **TradeStats — интеграция** в `unified_scanner` (поле `disb`)
- [ ] **A/B тест:** baseline / +TradeStats / +TradeStats+HI2
- [ ] **Бэктест стопов** (`backtest_stop_levels.py`)
- [ ] **Индивидуальный ATR** по тикерам (CE, RB, MG — волатильные)
- [ ] **`/fq/hi2/`** (404) — запросить AlgoPack
- [ ] **Открытые позиции:** CC, CNYRUBF, KC, NR, USDRUBF

### ⚠️ Открытые вопросы
1. **Патч `close_position`** — считать `pnl_points` при закрытии
2. **Дашборд** — формат отображения
3. **TradeStats** — какие поля использовать (главное `disb`)
4. **A/B тест** — как организовать (3 версии робота?)


## 15.09.2026 (дневная сессия — дашборд: PnL в пунктах + LQDT)

### ✅ Выполнено

**📊 ДАШБОРД — PnL В ПУНКТАХ:**
- **Патч `app_v2.py`:** в «Статистику сделок» добавлена колонка **PnL (пункты)**
- **Формат:** `+329 202.2₽` + `+4 778.6 pts` (рядом)
- **Расчёт:** `_fut_total_pts = _fut_closed_df['pnl_points'].sum()`
- **Проверка:** `grep -n "PnL (пункты)"` → строка 4717 ✅

**📊 ДАШБОРД — LQDT С RI / БЕЗ RI:**
- **Проблема:** RI (+325 594₽) = **98.9% PnL** — искажает **% доходности**
- **Было:** `Робот (доходность) = +329.20%` (формально, но вводит в заблуждение)
- **Стало:** две метрики — **«с RI»** (+329.20%) и **«без RI»** (+3.61%)
- **Дополнительно:** `st.caption("⚠️ RI — аномалия: +325 594₽ (98.9% PnL)")`
- **Проверка:** `grep -n "Робот (с RI)"` → строка 4764 ✅

**🔧 ОТКАТ БАГА (IndentationError):**
- **Первый патч** — сломал дашборд (неправильные отступы)
- **Причина:** `_fut_total_pts` — с отступом 0 (вместо 20)
- **Решение:** **откат бэкапа** + **патч целым блоком** (не построчно)
- **Урок:** патчи — **только целыми блоками**; отступы **критичны**

**📈 РАСХОЖДЕНИЯ ДАШБОРДА И БД — ПРОВЕРЕНО:**
- **Win Rate:** 12/42 = 28.6% ✅ (дашборд прав)
- **Общий PnL:** +329 202.24₽ ✅
- **Средний PnL (прибыльные):** +27 604.37₽ ✅
- **Средний PnL (убыточные):** −68.34₽ ✅
- **Причина расхождения (12 vs 13):** **ME (−99₽) в EXPIRY** — 1 убыточная из 9

**🔍 АНАЛИЗ RI-АНОМАЛИИ:**
- **RI:** +325 594.09₽ = **98.9% PnL**
- **RN:** +2 563₽ = 0.78%
- **TT:** +2 128₽ = 0.65%
- **Без RI:** PnL = **+3 608.15₽** (3.61% от депозита)
- **LQDT:** +0.18%
- **Робот (без RI) опережает LQDT на +3.43%**

**📊 СТАТИСТИКА ПО exit_reason:**
| exit_reason | cnt | wins | losses | pnl |
|-------------|-----|------|--------|-----|
| EXPIRY | 9 | 8 | 1 | +330 838.59 |
| EXPIRY_MANUAL | 2 | 2 | 0 | +186.88 |
| SIGNAL | 2 | 2 | 0 | +128.02 |
| STOP | 29 | 0 | 29 | −1 951.26 |
| **ИТОГО** | **42** | **12** | **30** | **+329 202.24** |

**Git:**
- Коммит `d16ee5b` — dashboard: PnL в пунктах + LQDT с/без RI
- Запушен в `origin/master`

### 📝 Заметки
- **Streamlit:** hot-reload — **не нужен перезапуск** при изменении `app_v2.py`
- **Лог:** `/root/finlab/logs/streamlit.log`
- **Порт:** 8501 (LISTEN 0.0.0.0)
- **Депозит:** `_deposit = st.sidebar.number_input("Депозит (₽)", value=100000)` — **100 000₽** по умолчанию
- **LQDT_D1.parquet:** 79 записей, last = 14.09.2026 (close=2.0849)
- **RI — аномалия:** >90% PnL — показывать отдельно

### 🎯 На следующий раз
- [ ] **Патч `close_position`** — считать `pnl_points` в коде (для будущих сделок)
- [ ] **VK-уведомления** — формат `+325 594.09₽ (+1 930 пунктов)`
- [ ] **TradeStats — интеграция** в `unified_scanner` (поле `disb`)
- [ ] **A/B тест:** baseline / +TradeStats / +TradeStats+HI2
- [ ] **Бэктест стопов** (`backtest_stop_levels.py`)
- [ ] **Индивидуальный ATR** по тикерам (CE, RB, MG — волатильные)
- [ ] **`/fq/hi2/`** (404) — запросить AlgoPack
- [ ] **Открытые позиции:** CC, CNYRUBF, KC, NR, USDRUBF
- [ ] **`use_container_width`** → `width='stretch'` (deprecation warning)

### ⚠️ Открытые вопросы
1. **Патч `close_position`** — считать `pnl_points` при закрытии
2. **VK-уведомления** — формат отображения
3. **TradeStats** — какие поля использовать (главное `disb`)
4. **A/B тест** — как организовать (3 версии робота?)
5. **`use_container_width`** — deprecated с 2025-12-31, обновить


## 15.09.2026 (вечерняя сессия — HI2: финальный разбор + AlgoPack)

### ✅ Выполнено

**📧 ОТВЕТ ALGOPACK (15.09 в 10:23):**
- **Цитата:** *«12, 13 число — это неторговые выходные. За 15 число данные будут рассчитаны только в 19:00 сегодня. В запросе с 11 сентября по 15 сентября должно быть две даты 2026-09-11 и 2026-09-14. В сообщении упоминаются эндпоинты и логи. Если проблема сохраняется, просьба их приложить.»*
- **Вывод:** **API работает**, данные приходят с задержкой (19:00 МСК).
- **AlgoPack приложил примеры** для SiU6, BRV6, RIU6 — 11 метрик HI2!

**📊 НОВЫЕ МЕТРИКИ HI2 (11 штук!):**
- `hhi_agressive`, `hhi_agressive_buy`, `hhi_agressive_sell`
- `hhi_buy`, `hhi_sell`
- `hhi_netflow_buy`, `hhi_netflow_sell`
- `hhi_passive`, `hhi_passive_buy`, `hhi_passive_sell`
- `hhi_volume`

**🎯 FO (фьючерсы) — ПРОВЕРЕНО:**
- **С длинным кодом** — **РАБОТАЕТ:**
  - LKU6, RNU6, TNU6, SiU6, RIU6, BRV6, CEU6, SNU6, NAU6, PTU6 → **rows=33**
- **С коротким кодом** — **НЕ работает:**
  - LK, RN, TN, HY, IR, GD, CE, SN, NA, PT → **rows=0**
- **Вечные фьючерсы** — **РАБОТАЮТ:**
  - GAZPF, SBERF, USDRUBF, CNYRUBF → **rows=33**

**🎯 Валютные (FO):**
- USDRUBF, CNYRUBF, EURRUBF → **rows=33** ✅

**📊 HI2 — ФИНАЛЬНАЯ ПРОВЕРКА:**
- **`hi2_daily.parquet`:** 5415 строк, last=14.09.2026
- **Per-ticker файлы:**
  - LK_hi2: 330 строк, last=14.09
  - CE_hi2: 286 строк, last=14.09
  - RN_hi2: 319 строк, last=14.09
  - GAZPF: 330 строк, last=14.09
  - SBERF: 308 строк, last=14.09
- **Ручной запуск:** 52 440 записей (все тикеры)

**🎯 ПОЧЕМУ 15.09 НЕ ПОЯВИЛОСЬ:**
- **AlgoPack:** данные за 15.09 — **после 19:00 МСК**
- **Сейчас 16:13** — сбор **до 14.09** (норма)
- **После 19:00** — данные за 15.09

**📋 `_resolve_full_code` — УЖЕ В КОДЕ:**
- Строка 75: `def _resolve_full_code(short_code)`
- Строка 113: `api_ticker = self._resolve_full_code(ticker) if engine == "futures" else ticker`
- **Работает** — для futures берёт длинный код из `contract_cache.json`

**📊 `contract_cache.json`:**
- LK → LKU6, RN → RNU6, TN → TNU6, HY → HYU6, IR → IRU6
- CE → CEZ6, SN → SNU6, NA → NAU6, PT → PTU6, GD → GDU6
- **Дата:** 2026-09-15

### 📝 Заметки
- **HI2 работает**, данные **до 14.09** (15.09 — после 19:00)
- **FO** — длинный код обязателен (`LKU6`, не `LK`)
- **Вечные фьючерсы** — короткий код (`GAZPF`, `USDRUBF`)
- **11 метрик HI2** (не 6!) — богаче, чем думали
- **DeprecationWarning:** `is_in` с collection — обновить на `implode`

### 🎯 На следующий раз
- [ ] **Проверить HI2 после 19:00** — данные за 15.09
- [ ] **Использовать 11 метрик HI2** в unified_scanner (не только 2)
- [ ] **Патч `close_position`** — `pnl_points` в коде
- [ ] **VK-уведомления** — формат `+325K₽ (+1930 pts)`
- [ ] **TradeStats** — интеграция
- [ ] **A/B тест**
- [ ] **Бэктест стопов**


## 15.09.2026 (вечерняя сессия 2 — HI2: 11 метрик)

### ✅ Выполнено

**📊 HI2 — ВСЕ 11 МЕТРИК (было 2):**
- **Проблема:** `hi2_daily_aggregator.py` — **только 2 метрики** (`hhi_agressive`, `hhi_volume`)
- **В сырых данных** (`data/hi2/*.parquet`) — **11 метрик**!
- **Патч агрегатора:**
  - Добавлена константа `METRICS` (11 метрик)
  - `agg_exprs` через `list comprehension`
  - `hi2_daily.parquet`: **14 колонок** (было 5)

**📊 11 МЕТРИК HI2:**
- `hhi_agressive`, `hhi_agressive_buy`, `hhi_agressive_sell`
- `hhi_buy`, `hhi_sell`
- `hhi_netflow_buy`, `hhi_netflow_sell`
- `hhi_passive`, `hhi_passive_buy`, `hhi_passive_sell`
- `hhi_volume`

**📈 АНАЛИТИКА ЗА 14.09:**
- **Топ-10 по `hhi_agressive`:** AN=8544, MG=4274, SQ=3652, KRKNP=3338, ELMT=3176, LD=2801, VB=2764, ME=2742, NM=2642, SC=2630
- **Топ-5 агрессивных покупателей (ratio buy/sell):** GCHE=5.01, YAKG=4.83, RASP=2.61, SH=2.59, SELG=2.47
- **Топ-5 агрессивных продавцов:** ZAYM=0.08, TGKA=0.14, MSNG=0.16, DOMRF=0.21, ABIO=0.22

**Git:**
- Коммит `1705092` — hi2_aggregator: все 11 метрик HI2
- Коммит `144c184` — WORK_LOG: HI2 — 11 метрик в агрегате
- Запушены в `origin/master`

### 📝 Заметки
- **11 метрик HI2** — богатство для анализа
- **`hhi_agressive_buy/sell`** — направление агрессии
- **`hhi_netflow_buy/sell`** — чистый поток
- **`hhi_passive_buy/sell`** — пассивная ликвидность
- **`hhi_volume`** — объём

### 🎯 На следующий раз
- [ ] **Интеграция 11 метрик** в `unified_scanner`
- [ ] **Патч `close_position`** — `pnl_points` в коде
- [ ] **VK-уведомления** — формат `+325K₽ (+1930 pts)`
- [ ] **TradeStats** — интеграция
- [ ] **A/B тест**
- [ ] **Бэктест стопов**
- [ ] **Проверить HI2 за 15.09** (после 19:00)


## 15.09.2026 (вечерняя сессия — стопы фьючерсного робота)

### ✅ Выполнено

**Проблема:** фьючерсный робот закрывал большинство сделок по STOP с минимальными убытками (whipsaw).

**Причина:**
- Стоп 2×ATR — слишком близко
- Проверка каждые 10 минут по M10 (high/low) — малейший вылет закрывает позицию
- Нет буфера
- Нет безубытка

**Решение:**
- `STOP_ATR_MULT = 3.2` (3×ATR + 0.2×ATR буфер)
- `BE_MOVE_ATR = 1.5` (при движении в плюс на 1.5×ATR — стоп в безубыток)
- `exit_reason = BREAKEVEN` при закрытии по безубытку
- Убрано дублирование стопа из `main()`

**Патчи `futures_robot.py`:**
- `open_position`: записывает `stop_price` (3.2×ATR)
- `close_position`: `pnl_points` и `point_value` в расчёте и UPDATE
- `check_stops_only`: `conn` + `cursor` для обновления `stop_price`
- `check_stops_only`: 3.2×ATR + безубыток
- `main`: убрано дублирование стопа

**Git:**
- Коммит `2a21361` — «Стопы: 3.2×ATR + безубыток + pnl_points + conn fix»
- Запушен в `origin/master` и `finlab-dashboard/master`

### 🎯 На следующий раз
- [ ] 11 метрик HI2 в `unified_scanner`
- [ ] TradeStats в `unified_scanner` (поле `disb`)
- [ ] Патч `close_position` в парном роботе (`leg_a_pnl_points`, `leg_b_pnl_points`, `total_pnl_points`)
- [ ] Наблюдать за стопами — снизился ли whipsaw


## 15.09.2026 (ночная сессия — 4 задачи)

### ✅ Выполнено

**Задача 1: Стопы фьючерсного робота**
- `STOP_ATR_MULT = 3.2` (3×ATR + 0.2×ATR буфер)
- `BE_MOVE_ATR = 1.5` (стоп в безубыток при движении в плюс на 1.5×ATR)
- `exit_reason = BREAKEVEN` при закрытии по безубытку
- `open_position`: записывает `stop_price`
- `close_position`: `pnl_points`, `point_value`
- `check_stops_only`: `conn` + `cursor` для обновления `stop_price`
- Убрано дублирование стопа из `main()`
- Backfill `stop_price` для 4 открытых позиций (PD, PT, RI, SBERF) + NR
- Коммиты: `2a21361`, `371a2af`

**Задача 2: 11 метрик HI2 в unified_scanner**
- Расширена сигнатура: `hi2_agressive_buy`, `hi2_agressive_sell`, `hi2_buy`, `hi2_sell`, `hi2_netflow_buy`, `hi2_netflow_sell`, `hi2_passive`, `hi2_passive_buy`, `hi2_passive_sell`, `hi2_volume`
- Пересмотрены пороги под реальные данные (median=560): >4000, >2500, >1500
- Модификаторы: перекос агрессивных ×2 (±3), netflow ×3 (±2), объём >1500 (-3)
- `futures_robot`: `get_hi2_for_ticker()` + передача 11 метрик
- Коммит: `dcfc228`

**Задача 3: *_pnl_points в парном роботе**
- `close_position`: `leg_a_pnl_points`, `leg_b_pnl_points`, `total_pnl_points`
- Формула: `(price - entry) × volume` (без point_value)
- Коммит: `e99baf4`

**Задача 4: TradeStats disb**
- `unified_scanner`: параметр `disb` + модификатор (Вариант C)
- Пороги: >0.5 (+3), >0.2 (+1), <-0.5 (-3), <-0.2 (-1)
- `futures_robot`: `get_disb_for_ticker()` + передача `disb`
- Коммит: `6f6f0f5`

### 🎯 На следующий раз
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2
- [ ] Бэктест стопов (`backtest_stop_levels.py`)
- [ ] Индивидуальный ATR по тикерам (CE, RB, MG — волатильные)
- [ ] Наблюдать за стопами — снизился ли whipsaw после 3.2×ATR + безубытка
- [ ] Проверить, что HI2 и disb реально влияют на скор (в логах)


## 16.09.2026 (дневная сессия — стратегия тестирования)

### 📌 ПРИНЯТЫЕ РЕШЕНИЯ

**Проблема:** фьючерсный робот — Win Rate 10.5%, сигналы `fiz_buy` не работают.

**Решение:** перейти на `yur_buy_ratio` (юрлица) + параллельное тестирование.

### 🎯 СТРАТЕГИИ

**Основная: C+D (группы + динамические пороги)**
- Группы: Индексы, Акции, Товары, Валюты
- `dir` — индивидуально для каждого тикера
- Пороги (median/std) — из группы
- **Напоминалка:** если результаты плохие → всё индивидуально (dir + пороги)

**Тестовая: E (мультифакторная)**
- Веса: yur_buy (30%), HI2 (20%), disb (20%), GARCH (10%), Тренд (10%), Объём (10%)
- **Напоминалка:** пересмотреть веса после результатов

**Отложено:** C (только группы), D (только динамические пороги)
- **Напоминалка:** добавить позже, если понадобится

### 📋 ПЛАН

**Этап 1: Бэктест** (сначала)
- Прогнать исторические данные через C+D и E
- Сравнить Win Rate, PnL, Sharpe
- **Напоминалка:** сделать бэктест

**Этап 2: Параллельный запуск** (2 робота)
- `futures_robot.py` — C+D (основная)
- `futures_robot_e.py` — E (тест)
- Виртуальный депозит
- Срок: 1–2 месяца (или до 30+ сделок)
- **Напоминалка:** запустить 2 робота

**Этап 3: Анализ** (после накопления данных)
- Сравнить результаты
- Решить: какая стратегия — основная
- **Напоминалка:** проанализировать результаты

### 📊 ТЕКУЩИЕ ДАННЫЕ

**`yur_buy` работает:**
- D1: RI (-0.51), SBERF (+0.37), GZ (+0.35), MG (+0.31), SN (+0.27), CE (+0.14), GD (-0.02), GAZPF (+0.23)
- H4: GD (-0.51), SBERF (-0.47), GZ (+0.43), SN (+0.36), CE (+0.30), RI (-0.25)
- H1: слабее

**`signal_direction.json`** — создан (dir, median, std для 8 тикеров)
**`yur_stats.json`** — создан (63 тикера)

### 🎯 НА СЛЕДУЮЩИЙ РАЗ
- [ ] Бэктест C+D и E
- [ ] Перестроить `get_tf_signal` на `yur_buy`
- [ ] Создать `futures_robot_e.py`
- [ ] Запустить параллельно 2 робота

### 📝 НАПОМИНАЛКИ
- [ ] Если C+D плохо → всё индивидуально (dir + пороги)
- [ ] Пересмотреть веса E после результатов
- [ ] Добавить C и D отдельно позже
- [ ] Проверить группировку (4 группы: Индексы, Акции, Товары, Валюты)

---


## 16.09.2026 — Анализ подходов к сигналам (A/B/C/D/E)

### Подход A: «Индивидуальные настройки»

**Плюсы:**
- Точность — каждый тикер оптимизирован
- Высокая корреляция (0.3–0.5)

**Минусы:**
- Overfitting — на истории работает, в бою — нет
- Сложность — 63 тикера, 63 настройки
- Противоречит Саймонсу — «один алгоритм»
- Риск: если рынок изменится, настройки устареют

### Подход B: «Единый алгоритм» (Саймонс)

**Плюсы:**
- Системность — один алгоритм для всех
- Устойчивость — не переобучается
- Простота — одна логика

**Минусы:**
- Слабая корреляция (~0.02)
- Не работает на текущих данных

### Подход C: «Группировка по классам активов» (компромисс)

**Идея:** разбить тикеры на группы по типу рынка:
- Индексы (RI, MX, ...)
- Акции (SBERF, GAZPF, LK, ...)
- Товары (SN, MG, GD, ...)
- Валюты (CNYRUBF, USDRUBF, ...)

Для каждой группы — свои правила.

**Плюсы:**
- Меньше настроек (4–5 групп)
- Логично — разные рынки ведут себя по-разному
- Устойчивость — не переобучается на каждом тикере

**Минусы:**
- Внутри группы — разброс (например, SBERF и GAZPF — обе акции, но корреляции разные)

### Подход D: «Динамические пороги» (адаптивность)

**Идея:** не фиксированные пороги, а адаптивные:
- Скользящее окно (например, 100 дней)
- Z-score относительно текущего распределения

Для каждого тикера — своя медиана и std, но они обновляются каждую неделю.

**Плюсы:**
- Адаптивность — учитывает изменения рынка
- Нет overfitting — не фиксируем параметры навсегда
- Автоматизация — cron-задача обновляет статистику

**Минусы:**
- Сложнее — нужна инфраструктура
- Задержка — статистика обновляется раз в неделю

### Подход E: «Комбинация факторов» (мультифакторная модель)

**Идея:** yur_buy — один из факторов. Использовать комбинацию:
- yur_buy (30%)
- fiz_buy (10%, как контр-индикатор)
- HI2 (20%)
- TradeStats disb (20%)
- GARCH (10%)
- Тренд (10%)

Для каждого фактора — свой вклад, но без индивидуальных настроек.

**Плюсы:**
- Устойчивость — комбинация снижает шум
- Гибкость — можно добавлять/убирать факторы
- Прозрачность — видно вклад каждого фактора

**Минусы:**
- Сложно оптимизировать веса
- Нужно много данных для обучения

### 🎯 РЕШЕНИЕ

**Основная:** C+D (группы + динамические пороги)
**Тестовая:** E (мультифакторная)
**Отложено:** A (индивидуальные), B (единый)

---


## 16.09.2026 — ЗАДАЧА: Проверить логику парного робота

### 📌 ПРИЧИНА

В фьючерсном роботе обнаружены **логические ошибки**:
- merge H4/1H не работал (fiz_buy = 50)
- `get_tf_signal` — инвертированная логика
- `fiz_buy` — не работает (корреляция ~0)

**Возможно, в парном роботе — те же проблемы.**

### 🎯 ЧТО ПРОВЕРИТЬ в `pairs_robot.py`

**1. `calculate_zscore`**
- Как считается z-score спреда
- Окно (20 свечей) — правильное?
- Нормализация (mean/std) — корректная?

**2. `open_position`**
- Как выбирается направление (LONG_SPREAD / SHORT_SPREAD)
- Правильно ли определяется `leg_a_direction`, `leg_b_direction`

**3. `close_position`**
- Как считаются `leg_a_pnl`, `leg_b_pnl`
- `pnl_points` — правильная формула
- `point_value` — для каждой ноги

**4. `check_signals_by_tf`**
- Как формируются сигналы по ТФ (M10, H1, H4, D1)
- Не инвертированы ли сигналы (как в `unified_scanner`)

**5. Фильтры**
- Время (07:00–18:00 МСК)
- Волатильность (std > 2% от среднего)
- Не блокируют ли хорошие сделки

**6. `is_tf_fresh`**
- Проверка свежести M10, H1, H4, D1
- Пороги: M10=3ч, H1=4ч, H4=25ч, D1=25ч

**7. `is_moex_trading_day`**
- Календарь MOEX
- Неторговые дни

**8. `pairs_config.json`**
- Какие пары включены (`enabled: true`)
- Какие отключены

**9. Свежесть данных**
- M10, H1, H4, D1 — обновляются ли

**10. `point_value` для ног**
- Правильно ли определяется для каждого тикера

### 📝 ПЛАН ПРОВЕРКИ

1. **Прочитать код** `pairs_robot.py` полностью
2. **Проверить `calculate_zscore`** — на исторических данных
3. **Проверить `check_signals_by_tf`** — какие сигналы даёт
4. **Проверить `close_position`** — формулу PnL
5. **Проверить фильтры** — не блокируют ли
6. **Сравнить с фьючерсным роботом** — что общего, что разного
7. **Написать тесты** — как для фьючерсного

### 🎯 НАПОМИНАЛКА

- [ ] **Проверить логику парного робота** (после бэктеста C+D и E)
- [ ] Проверить `calculate_zscore`
- [ ] Проверить `check_signals_by_tf`
- [ ] Проверить `close_position`
- [ ] Проверить фильтры

---

## ВАЖНО: ПРАВИЛО — ПУШИТЬ WORK_LOG СРАЗУ

### ПРОБЛЕМА (16.09.2026)

**Ситуация:** в другой ветке (чате) была прочитана устаревшая информация из WORK_LOG и README.

**Причина:**
- Мы обновляли WORK_LOG локально на сервере
- Но не пушили в GitHub сразу
- Другой чат читал старую версию с GitHub
- Информация оказалась устаревшей (12.09 вместо 16.09)

**Последствия:**
- Напарник был неприятно удивлён
- Другой AI получил неверную картину проекта
- Пришлось разбираться

### РЕШЕНИЕ — ЖЁСТКОЕ ПРАВИЛО

**ПОСЛЕ КАЖДОГО ИЗМЕНЕНИЯ WORK_LOG — СРАЗУ:**

    cd /root/finlab
    git add -f WORK_LOG.md
    git commit -m "WORK_LOG: ..."
    git push origin master
    git push finlab-dashboard master

**НЕ ОТКЛАДЫВАТЬ ПУШ!**
**НЕ НАДЕЯТЬСЯ, ЧТО "ПОТОМ ЗАПУШИМ"!**

### КОНТРОЛЬНЫЙ СПИСОК (в конце каждой сессии)

- [ ] git status — clean?
- [ ] git log --oneline -3 — HEAD = origin/master?
- [ ] git log origin/master --oneline -3 — совпадает с HEAD?
- [ ] WORK_LOG запушен?
- [ ] README запушен (если менялся)?

### НАПОМИНАЛКА

**Каждое изменение WORK_LOG → сразу коммит → сразу пуш!**

**Не важно, закончена ли сессия. Не важно, устал ли. Не важно, что ещё есть дела.**

**ПРАВИЛО: изменил WORK_LOG — запушил.**

### ПРИЧИНА

**Другой AI-чат читает README и WORK_LOG С GITHUB.**
**Если не запушить — он получит устаревшую информацию.**
**Это сбивает с толку и создаёт проблемы.**

**Лучше запушить 10 раз за сессию, чем 1 раз забыть.**

### ПРОВЕРКА ПОСЛЕ ПУША

    cd /root/finlab
    git status
    git log origin/master --oneline -3

---

## ВАЖНО: ПРАВИЛО — ПРОВЕРЯТЬ ДУБЛИКАТЫ ПЕРЕД КОММИТОМ

### ПРОБЛЕМА (16.09.2026)

**Ситуация:** в WORK_LOG попали дубликаты:
- Август (02.08, 03.08) — 2 копии
- 04.09, 06.09, 08.09 — по 2 копии

**Причина:**
- Мы дважды добавили август (он уже был, и мы добавили ещё)
- Кто-то ранее копировал записи 04.09, 06.09, 08.09

### РЕШЕНИЕ — ЖЁСТКОЕ ПРАВИЛО

**ПЕРЕД КАЖДЫМ КОММИТОМ WORK_LOG:**

1. **Проверить, есть ли уже такая запись:**
   grep "## 16.09.2026" /root/finlab/WORK_LOG.md

2. **Проверить дубликаты заголовков:**
   grep "^## " /root/finlab/WORK_LOG.md | sort | uniq -d

3. **Проверить git diff:**
   cd /root/finlab && git diff WORK_LOG.md | head -50

4. **Вставлять только в КОНЕЦ файла:**
   cat >> /root/finlab/WORK_LOG.md << 'EOF'
   ...
   EOF
   (НЕ использовать `cat >` — перезапись, НЕ использовать `sed` — в начало)

### КОНТРОЛЬНЫЙ СПИСОК

- [ ] Нет дубликатов заголовков? (`grep "^## " | sort | uniq -d`)
- [ ] Запись добавлена в КОНЕЦ файла?
- [ ] git diff показывает только добавление?
- [ ] После коммита — сразу пуш?

### НАПОМИНАЛКА

**Правило:**
- **Перед добавлением** — проверить, есть ли запись
- **Использовать git diff** для проверки
- **Вставлять только в конец** (`>>`), не в начало
- **Проверять grep на дубликаты** перед коммитом

### ПРИЧИНА

**Дубликаты сбивают AI в других чатах.**
**Они видят "мусор" и считают файл повреждённым.**
**Это создаёт проблемы и тратит время.**

---

## 16.09.2026 (вечерняя сессия — починка парного робота)

### ✅ Выполнено

**🔴 Баг #1: `NameError: ENTRY_Z_DEFAULT` в pairs_robot.py**
- Обнаружено: 4909 ошибок в `robot.log` (все пары на M10, H1, H4)
- Причина: при патче cooldown (13.09) или time filter (16.09 11:07) случайно удалены 3 константы: `ENTRY_Z_DEFAULT`, `EXIT_Z_DEFAULT`, `MAX_POSITIONS`
- Также отсутствовала `COOLDOWN_HOURS` (использовалась в `is_pair_in_cooldown`, но не определена)
- Исправлено: вставлены 4 константы после `CHECK_INTERVALS` (строки 51-54):
  - `ENTRY_Z_DEFAULT = 3.0`
  - `EXIT_Z_DEFAULT = 0.5`
  - `MAX_POSITIONS = 10`
  - `COOLDOWN_HOURS = 4`

**🔴 Баг #2: `NameError: timedelta` в pairs_robot.py**
- Обнаружено после фикса бага #1: все пары падали с `name 'timedelta' is not defined`
- Причина: `from datetime import datetime` — БЕЗ `timedelta`, а `timedelta` используется в `is_pair_in_cooldown` (строка 545)
- Исправлено: `from datetime import datetime, timedelta` (строка 17)

**✅ Подтверждено (проверено в futures_robot.py и unified_scanner.py):**
- Стопы 3.2×ATR + безубыток (`STOP_ATR_MULT=3.2`, `BE_MOVE_ATR=1.5`)
- Cooldown 4ч (`COOLDOWN_HOURS=4`, `is_in_cooldown`)
- HI2 — передаётся (11 метрик)
- TradeStats `disb` — работает (`unified_scanner.py` строки 169-186)
- `backtest_stop_levels.py` + `backtest_individual_atr.py` — есть (16.09 11:29)

**✅ Ручное закрытие 4 старых позиций парного робота (Entry=2.0, открыты 11.09):**
| id | pair | entry_z | PnL |
|----|------|---------|-----|
| 48 | SNGSP-SG_M10 | -2.02 | +586.63₽ |
| 49 | SVCB-SC_M10 | 1.64 | -24.76₽ |
| 50 | LKOH-HYDR_M10 | -2.08 | +79.01₽ |
| 52 | SOFL-S0_H1 | 1.80 | -5.44₽ |
| **Итого** | | | **+635.44₽** |

**Осталось 2 открытых:** 46 (GAZPF-GZ_M10, 3.22), 55 (WUSH-WU_M10, -2.28)

### 📝 Заметки
- `pairs_robot.py` использует `exit_z` (НЕ `exit_reason` — это в `futures_positions`)
- `robot.log` — не в journalctl, а в файле `/root/finlab/robots/robot.log`
- Время 22:40 МСК — вне торгового окна (7:00-18:00), робот не открывает новые

### 🎯 На следующий раз
- [ ] Подключить `yur_buy_ratio` в `unified_scanner` (есть `signal_direction.json` — 8 тикеров)
- [ ] `MAX_POSITIONS` — поднять до 15?
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2
- [ ] Запустить `backtest_stop_levels.py` — результаты
- [ ] Проверить, что робот открывает новые позиции в торговое окно (17.09)


## 17.09.2026 (вечерняя сессия — rollover, RI, фильтр корреляции)

### ✅ Выполнено

**🔴 RI — реальный убыток −341 644.84₽ (STOP)**
- id 18 (RIU6, EXPIRY): +325 594.09₽ (закрыт 15.09 04:09)
- id 52 (RIZ6, STOP): −341 644.84₽ (открыт 15.09 10:59, закрыт 17.09 07:03)
- **Причина:** робот переоткрыл RI уже в новом контракте RIZ6 после rollover (ATR вырос с 337 до 633)
- **PnL корректен** (point_value=168.7016, volume=1.0)

**✅ Миграция `contract_code`**
- `ALTER TABLE futures_positions ADD COLUMN contract_code TEXT`
- Заполнено для 9 открытых позиций: NRU6, PTU6, PDU6, SVU6
- Вечные фьючерсы (USDRUBF, CNYRUBF, SBERF, GAZPF, GLDRUBF) → `?`
- **Зачем:** различать контракты при rollover, корректный бэктест

**✅ Патч `pairs_config.json` — entry_z**
- SOFL-S0_H1: entry_z 1.5 → **2.5**, exit_z 0.0 → **0.5**, window 20 → **40**
- SVCB-SC_M10: entry_z 1.5 → **2.5**
- Бэкап: `pairs_config.json.bak_entryz_20260917_201659`

**✅ Фильтр корреляции в `pairs_robot.py`**
- Функция `calculate_correlation(df_a, df_b, window=20)` — Pearson (строка 274)
- В `check_signals_by_tf`: если corr < 0.7 → не входить (строки 679-685)
- **Зачем:** защита от нестабильных пар

**✅ VK API — разобран `error_code: 40`**
- Ручной запуск работает (`{'response': 978}`)
- Cron падает **редко** (1 раз при rollover ~100 контрактов) — **rate limit** VK
- **Не критично** — уведомления от роботов работают

**📊 PnL по роботам (за всё время)**
- Фьючерсный: 48 сделок, **+329 202₽** (RI — 98.9% — аномалия)
- Парный: 23 сделки, **+787.57₽** (WR 56.5%)
- **Лидеры фьючерс:** RN (+2563₽), TT (+2128₽), SP (+485₽)
- **Убыточные:** RI (−16050₽), CE (−597₽), SN (−245₽)

### 📝 Заметки
- RI rollover: RIU6 → RIZ6 (декабрьский)
- Вечные фьючерсы: SECID = тикер (нет U6/Z6)
- `contract_cache.json`: 169 контрактов
- VK `error_code: 40` — rate limit, не баг

### 🎯 На следующий раз
- [ ] Подключить `yur_buy_ratio` в `unified_scanner`
- [ ] `MAX_POSITIONS` — 10 → 15?
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2
- [ ] Запустить `backtest_stop_levels.py`
- [ ] Walk-forward оптимизация для `pairs_optimizer.py`
- [ ] Минимум 20 сделок для оптимизации пар

### 🔧 Дополнение 17.09 (продолжение вечерней сессии)

**✅ Подключён `yur_buy_ratio` в `unified_scanner.py`**
- Аргументы: `yur_buy_ratio`, `yur_dir`, `yur_median`, `yur_std` (строка 56)
- Логика `yur_mod` (строки 189-215): dir=1 → +2/+1/−2, dir=−1 → +2/+1/−2
- `total_mod` обновлён (с `yur_mod`)
- `factors` (основной return, строка 404): `yur_mod`, `yur_note`
- `factors` (default return, строка 96): `yur_mod: 0`, `yur_note: '—'`
- **Зачем:** использовать сигнал (README 16.09: корреляция D1 0.27–0.51)

**✅ Подключён `yur_buy_ratio` в `futures_robot.py`**
- Загрузка `signal_direction.json` (строка 44)
- Функция `get_yur_params(ticker)` (строка 52)
- Чтение `yur_buy_ratio` из `df_1h` (строки 637-643)
- Передача в сканер (строка 662)

**✅ `MAX_POSITIONS` — 10 → 15 (только фьючерсный)**
- `futures_robot.py` строка 65

**📊 Проверка сборщиков и агрегаторов (check_collectors.py, 17.09 20:35)**

Сборщики (8):
- ✅ futoi_collect (0.6ч), candles_collect (0.5ч), hi2_collect (2.6ч), supercandles_collect (0.6ч)
- ✅ funding_collect (2.3ч), tradestats_collect (2.1ч), mega_alert (2.0ч), sector_indices (1.6ч)
- **Все: 0 ошибок**

Агрегаторы (6):
- ✅ futoi_1h (0.5ч), futoi_4h (0.4ч), futoi_daily (3.6ч)
- ✅ hi2_daily (2.5ч), supercandles_daily (3.5ч), supercandles_h4 (2.9ч)
- **Все: 0 ошибок**

**📝 Заметки**
- `check_collector_logs.py`: ✅ Ошибок не обнаружено
- `check_data_freshness.py`: ✅ Все данные актуальны
- `pair_signals_cron.log`: 0 ERROR (задача в cron закомментирована) — **не баг**
- `contract_check_cron.log`: 0 ERROR (старые 4 ERROR были VK rate limit)
- Тест `yur_mod` на RI: `NO_DATA` (в df нет колонки `begin`/`tradedate`) — **не баг кода**, в проде колонка есть

**🎯 Осталось**
- [ ] Проверить `yur_mod` в проде (в торговое окно 18.09)
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2 / +yur_buy_ratio
- [ ] `backtest_stop_levels.py` — запустить
- [ ] Walk-forward оптимизация

### 🔧 Дополнение 17.09 (проверка журнала сделок парного робота)

**✅ Проверка PnL в журнале сделок**
- PnL = leg_a_pnl + leg_b_pnl — **сходится** во всех записях ✅
- Формула `close_position` корректна:
  - BUY: `(exit - entry) × point_value × volume`
  - SELL: `(entry - exit) × point_value × volume`
- `point_value` для всех пар = 1.0 (кроме GAZPF = 0.01) ✅

**🔴 Найдены 2 проблемные сделки (id 28, 30)**
- `leg_a_pnl = 0`, `leg_b_pnl = 0`, но `pnl != 0` (ручная корректировка 11.09)
- **id 28** (SIBN-SO_M10): `leg_a=+2.95`, `leg_b=-19.00`, `pnl=-16.05`
- **id 30** (RASP-RA_H1): `leg_a=+0.20`, `leg_b=+4.00`, `pnl=+4.20`
- **Исправлено** пересчётом по формуле

**🔴 Дашборд: Win Rate некорректен (29.1% вместо 61.5%)**
- Причина: в 2 местах (`_closed_df`, `_pos_df`) не было фильтра legacy
- Legacy-сделки (29 шт, `exit_price = 0`) попадали в статистику
- **Исправлено:** добавлен фильтр `AND (exit_price_a != 0 AND exit_price_b != 0)` в строки 4420, 4581
- **Win Rate:** 61.5% (16 из 26) — корректно

**📊 Статистика парного робота (правильная)**
- Всего: 26 сделок
- Прибыльных: 16
- Убыточных: 10
- **Win Rate: 61.5%**
- **Общий PnL: +978.32₽**
- Средний PnL (прибыльные): +68.14₽
- Средний PnL (убыточные): −11.19₽

**📝 Заметки**
- Дашборд — **не через systemd** (запускается вручную)
- Legacy-сделки: 29 шт, `pnl = 0` (аннулированы 11.09)
- `point_value` в паре: LK=1.0, IR=1.0, SNGSP=1.0, SG=1.0, SIBN=1.0, SO=1.0

**🎯 Осталось**
- [ ] Перезапустить дашборд вручную
- [ ] Проверить в браузере (Win Rate = 61.5%)


## 18.09.2026 (вечерняя сессия — защита от экспирации)

### ✅ Выполнено

**🔴 Проблема: робот открывал позиции в контрактах без проверки экспирации**
- Правило «закрывать за 2 дня» — было (`check_expiry`)
- Правило «не открывать за 2 дня» — **отсутствовало**
- `expiry_date` **не заполнялась** при `open_position`
- `contract_code` **не заполнялся** для новых позиций (id 62-70)

**✅ Добавлены функции в `futures_robot.py`:**
- `get_last_tradedate(ticker)` — читает `contract_cache.json` + `contract_last_tradedate.json`, возвращает `date`
- `is_expiring_soon(ticker, days=2)` — True, если экспирация ≤ N дней
- Строки 67, 89

**✅ Патч `open_position`:**
- Заполняет `contract_code` (из `contract_cache.json`)
- Заполняет `expiry_date` (из `get_last_tradedate`)
- Строки 358-360

**✅ Патч `main()`:**
- Вызов `is_expiring_soon(ticker, days=2)` перед `open_position`
- Если True → **не открываем**

**✅ Создан кеш `contract_last_tradedate.json`:**
- 164 контракта (MOEX ISS)
- Обновление: cron `0 21 * * *`
- Скрипт: `scripts/update_last_tradedate.py`

**✅ Заполнены `contract_code` / `expiry_date` для открытых (id 42-70):**
- SV → SVZ6 (18.12), GD → GDZ6 (18.12), ED → EDZ6 (17.12)
- CR → CRZ6 (17.12), RB → RBZ6 (01.12), NG → NGU6 (28.09)
- PT → PTZ6 (18.12), NR → NRU6 (28.09)
- Вечные (CNYRUBF, SBERF, GAZPF, GLDRUBF) — без экспирации (норм)

**✅ NG — НЕ истекает сегодня:**
- MOEX ISS: NGU6 LASTTRADEDATE=2026-09-28
- Позиция корректна, закрывать не нужно

### 📝 Заметки
- `contract_last_tradedate.json` — кеш MOEX ISS (обновлять раз в день)
- `is_expiring_soon(ticker, days=2)` — защита от открытия
- `check_expiry()` — закрывает открытые за 2 дня
- Робот перезапущен (PID 3793032), без ошибок

### 🎯 На следующий раз
- [ ] Проверить работу `is_expiring_soon` в проде (19.09, торговое окно)
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2 / +yur_buy_ratio
- [ ] `backtest_stop_levels.py` — запустить
- [ ] Walk-forward оптимизация

### 🔧 Дополнение 18.09 (парный робот — защита от экспирации)

**🔴 Проблема: в парном роботе не было защиты от экспирации**
- 20 фьючерсов в парах (LK, RN, TN, IR, GAZPF, GZ, WU, SO, PH, ...)
- 4 из 7 открытых пар содержали фьючерс
- Правило «не открывать за 2 дня» — отсутствовало
- `expiry_date` не заполнялась

**✅ Миграция БД `positions`:**
- Добавлены: `contract_code_a`, `contract_code_b`, `expiry_date_a`, `expiry_date_b`

**✅ Функции в `pairs_robot.py` (скопированы из futures_robot):**
- `get_last_tradedate(ticker)` — из общего кеша `contract_last_tradedate.json`
- `is_expiring_soon(ticker, days=2)` — True, если экспирация ≤ 2 дней
- Строки 69, 89

**✅ Патч `open_position`:**
- Заполняет `contract_code_a/b`, `expiry_date_a/b` для обеих ног

**✅ Патч `check_signals_by_tf`:**
- Проверка `is_expiring_soon` для **обеих** ног перед открытием
- `_expiry_ok = False` → не открываем

**✅ Добавлена `check_expiry()`:**
- Проверяет **обе** ноги открытых пар
- Если **любая** истекает ≤ 2 дней → **закрывает пару**

**✅ Вызов `check_expiry()` в `main()`.**

**✅ Заполнены `contract_code_*` / `expiry_date_*` для открытых пар (7):**
- GAZPF-GZ_M10: A=GAZPF(—) / B=GZ(GZZ6, 17.12)
- WUSH-WU_M10: A=WUSH(—) / B=WU(WUZ6, 17.12)
- LKOH-HYDR_M10: LKOH / HYDR — обе акции
- LKOH-TATN_M10: LKOH / TATN — обе акции
- LKOH-IRAO_M10: LKOH / IRAO — обе акции
- SIBN-SO_M10: A=SIBN(—) / B=SO(SOZ6, 17.12)
- PHOR-PH_M10: A=PHOR(—) / B=PH(PHZ6, 17.12)

### 📝 Заметки
- Кеш `contract_last_tradedate.json` — общий для обоих роботов
- Вечные фьючерсы (CNYRUBF, GAZPF, GLDRUBF, SBERF, USDRUBF) — не в кеше, `expiry=None` (норм)
- `is_expiring_soon` возвращает False, если нет данных → не блокирует
- Робот перезапущен (PID 3794489), без ошибок

### 🎯 На следующий раз
- [ ] Проверить работу `is_expiring_soon`/`check_expiry` в проде (19.09, торговое окно)
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2 / +yur_buy_ratio
- [ ] `backtest_stop_levels.py` — запустить
- [ ] Walk-forward оптимизация


## 19.09.2026 (сессия — M10 каждые 10 мин, RI индивидуальный ATR)

### ✅ Выполнено

**✅ cron: candles_collector — каждые 10 мин**
- Было: `0 7-20 * * *` (раз в час)
- Стало: `*/10 7-20 * * *` (каждые 10 мин)
- **Зачем:** M10 — 10-минутные свечи, обновление каждые 10 мин — идеально
- **Эффект:** меньше устареваний

**✅ `stop_config.json` — добавлен RI: 2.5**
- Было: default = 3.2
- Стало: RI = 2.5 (индивидуальный)
- **Зачем:** RI — аномально волатильный (убыток −341 644₽ от 15.09)
- **Эффект:** стоп = 2.5 × ATR (вместо 3.2) — меньше убыток

**🔍 Cooldown — работает корректно**
- Парный: cooldown только после **убытка** (`pnl < 0`)
- Фьючерсный: cooldown только после **STOP**
- **Случай `SIBN-SO_M10`:** закрыт с +50.10₽ (прибыль) → cooldown **не применялся** (норм)
- **Случай `RI`:** STOP 17.09, cooldown 4ч прошёл → открыт 19.09 (правомерно)

**🔍 Данные устаревали — причина понятна**
- M10 — раз в час (порог 3ч)
- Если сборщик опаздывает → устаревают
- **Решение:** cron каждые 10 мин

**📊 RI — ATR**
- Текущий: 187.14 (0.22% от цены) — нормальный
- При открытии 15.09: 632.86 — аномальный (rollover RIU6 → RIZ6)
- **Решение:** индивидуальный множитель 2.5

### 📝 Заметки
- `candles_collector` в cron — с `PYTHONPATH=/root/finlab/FinLabPy`
- При ручном запуске — `PYTHONPATH` обязателен (иначе `ModuleNotFoundError: MOEXPy`)
- `stop_config.json` — 12 индивидуальных множителей (MG, MC, GZ, SN, NA, CE, GD, VB, PT, RL, HS, RI)

### 🎯 На следующий раз
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2 / +yur_buy_ratio
- [ ] `backtest_stop_levels.py` — запустить
- [ ] Walk-forward оптимизация
- [ ] Cooldown после прибыли — обсудить с командой

### 🔧 Дополнение 19.09 (A/B тест + walk-forward)

**✅ A/B тест unified_scanner (63 тикера)**
- Создан `scripts/ab_test_scanner.py`
- Результат: `yur_mod` меняет `score` от −2 до +2
- **НО:** `decision` не меняется (`tf_weighted` маленький)
- 6 тикеров дают не-WAIT: GL, MG, NR, SS (LONG), GZ, PT, VB (SHORT)

**🔍 tf_weighted — почему маленький**
- RI: `fiz_buy=62.3`, `fiz_delta=0` → NEUTRAL
- MG: `fiz_buy=75.2`, `fiz_delta=+0.06` → LONG
- GZ: `fiz_buy=81.4`, `fiz_delta=+0.88` → SHORT
- **Проблема:** `fiz_delta` часто 0 (merge не работает)
- **D1:** `fiz_buy=50` (merge не работает для D1)

**✅ Walk-forward для пар (40 пар, с комиссиями)**
- Создан `scripts/walk_forward_pairs.py`
- Комиссия 0.05% + slippage 0.02%, фильтр корреляции > 0.7
- In-sample 70% / out-sample 30%
- **OK: 29, FAIL: 11**

**FAIL-пары (out_pnl ≤ 0):**
- CNYRUBF-UM_M10 (out_wr=0.0)
- LKOH-ROSN_M10, LKOH-TATN_M10, ROSN-TATN_M10 (best_z=3.0)
- LKOH-IRAO_H1 (out_wr=51.0)
- LK-RN_M10 (out_wr=28.6)
- HY-IR_M10 (out_wr=53.6)
- LK-HY_H1 (out_wr=0.0)
- LK-TN_H1 (out_wr=50.0)
- RN-TN_H1 (out_wr=50.0)
- LK-IR_H1 (out_wr=77.8)

**Действие:** 11 FAIL-пар → `enabled: false`

**📊 Распределение entry_z (оптимум):**
- 1.5: 17 пар
- 2.0: 11 пар
- 2.5: 2 пары
- 3.0: 10 пар

**📝 Заметки**
- `yur_mod` — подтверждающий фактор (±2)
- `fiz_delta` — часто 0 (merge FutOI не работает)
- D1 — `fiz_buy=50` (merge не работает)
- Walk-forward — упрощён (без слияния спреда в 2 ноги)

**🎯 Осталось**
- [ ] Исправить A/B тест: D1 merge, fiz_delta
- [ ] Проверить `yur_mod` в проде (20.09)
- [ ] A/B: baseline / +TradeStats / +TradeStats+HI2 / +yur

---

## ИНСТРУКЦИЯ ДЛЯ AI В ДРУГИХ ЧАТАХ

### Проблема

**Raw-файлы GitHub кэшируются до 5 минут.**
**Если AI читает WORK_LOG сразу после пуша — он может получить старую версию.**

### Решение

**1. Использовать URL с ?t=<timestamp>:**
https://raw.githubusercontent.com/ArchakovBullet/supercandles-data/master/WORK_LOG.md?t=<timestamp>

**2. Или — GitHub API (без кэша):**
https://api.github.com/repos/ArchakovBullet/supercandles-data/contents/WORK_LOG.md?ref=master
В ответе — content в base64. Нужно декодировать.

**3. Проверить маркер версии:**
<!-- VERSION: 2026-09-16 22:00 MSK | COMMIT: 33349ba -->
**Если маркер отсутствует** — файл прочитан из кэша.

### Проверка

Через API (без кэша):
curl -s "https://api.github.com/repos/ArchakovBullet/supercandles-data/contents/WORK_LOG.md?ref=master" | python3 -c "import sys, json, base64; data = json.load(sys.stdin); content = base64.b64decode(data['content']).decode('utf-8'); print(content[:200])"

Через raw с timestamp:
curl -s "https://raw.githubusercontent.com/ArchakovBullet/supercandles-data/master/WORK_LOG.md?t=$(date +%s)" | head -5

### Источники

- README.md — паспорт проекта (редко меняется)
  https://raw.githubusercontent.com/ArchakovBullet/supercandles-data/master/README.md?t=<timestamp>

- WORK_LOG.md — дневник (обновляется каждую сессию)
  https://raw.githubusercontent.com/ArchakovBullet/supercandles-data/master/WORK_LOG.md?t=<timestamp>

### Важно

**Правило «пушить WORK_LOG сразу»** — см. выше в дневнике.
**Проверять дубликаты перед коммитом** — см. выше в дневнике.
**Использовать ?t=<timestamp> при чтении** — чтобы избежать кэша.

## 20.09.2026 (вечерняя сессия — защита от неторговых дней)

### 🔴 Проблема

**Фьючерсный робот открыл GL LONG (скор=76, цена=12288.30) в воскресенье 20.09.2026.**
- MOEX (фондовый рынок) не работает, но is_moex_trading_day() возвращала True.
- Робот считал день торговым → сканировал → находил сигнал → открывал позицию.

### 🔍 Причина

**is_moex_trading_day() — неполная во ВСЕХ роботах.**
- Функция проверяла только конкретные даты из no_trade_weekends и no_trade_holidays.
- Не проверяла now.weekday() → суббота и воскресенье считались торговыми (если не в списке).
- 20.09.2026 (вс) — не в списке → True → робот работал.

### ✅ Фиксы

**Фикс 1: is_moex_trading_day() в futures_robot.py (строка 148)**
    if now.weekday() >= 5:
        return False

**Фикс 2: is_moex_trading_day() в pairs_robot.py (строка 128)**
    if now.weekday() >= 5:
        return False

**Патч 1: open_position() в futures_robot.py (строка 336)**
    if not is_moex_trading_day():
        print(f'  неторговый день — позиция не открывается')
        return

**Патч 2: main() в futures_robot.py (строка 596)**
Перед for ticker in ready_tickers: — проверка is_moex_trading_day().

**Патч 3 v3: while running: в pairs_robot.py (строка 645)**
Внутри try:, перед cmd = process_command():
    if not is_moex_trading_day():
        print('Неторговый день — новые позиции не открываются')
        time.sleep(3600)
        continue

### 📊 Проверка

**is_moex_trading_day() в воскресенье 20.09 (19:59 МСК):**
Сейчас: 2026-09-20 19:59:13 Sunday
is_moex_trading_day(): False
Ожидалось: False — OK

**Логи фьючерсного робота:**
РОБОТ ФЬЮЧЕРСОВ | 2026-09-20 19:59:15
Проверяю 60 тикеров (из 165)...
Неторговый день — новые позиции не открываются
Ожидание 1 час... — OK

**Логи парного робота:**
РОБОТ ПАРНОЙ ТОРГОВЛИ | 2026-09-20 19:59:14
БД инициализирована
Неторговый день — новые позиции не открываются — OK

### 📝 Заметки
- is_moex_trading_day() — теперь учитывает сб/вс.
- check_stops_only() и check_expiry() — работают и в выходные.
- GL (id 75) — позиция открыта, закроется по стопу/сигналу в понедельник.
- Фильтр времени 10:00–18:00 — оставлен (это фильтр часов).

### 🎯 На следующий раз
- [ ] Проверить работу 21.09 (понедельник) — робот торгует нормально
- [ ] backtest_stop_levels.py — запустить
- [ ] Отключить 6 H4-пар по акциям (нет данных)
- [ ] A/B тест: baseline / +TradeStats / +TradeStats+HI2 / +yur_buy_ratio
- [ ] fiz_delta merge FutOI — исправить
