# Дневник разработки FinLabPy — 03.07.2026

## Тема: Кнопка Добавить, валидация тикеров, 8 срочных фьючерсов

### Реализовано

#### 1. Тест api.get_futoi() для разных форматов тикеров
- MOEX API для FutOI принимает ТОЛЬКО короткие коды: BR, SI, GD
- Полные коды (BRN6, SIM6, GDU6) возвращают 0 записей
- Candles RFUD принимает только полные коды
- HI2 futures принимает только полные коды

#### 2. Починили кнопку Добавить в сканер
- Скачивание свечей D1, H1, H4
- Обновление тикеров во всех сборщиках (futoi_collector, 1h/4h/daily агрегаторы, HI2)
- Автозапуск сборщика FutOI для нового тикера
- Валидация тикера: пробелы, спецсимволы, проверка через MOEX API
- Индексы не добавляются в FutOI сборщик

#### 3. Система уведомлений
- После добавления показывается полный статус всех шагов
- Toast-уведомление
- Expander с деталями (можно скрыть)

#### 4. Все 8 срочных фьючерсов добавлены
- SI, PT, VI, BR, GD, RI, MX, ED
- Для каждого: FutOI, свечи D1/H1/H4

#### 5. Исправлены баги
- Si -> SI (костыль в load_futoi_data и uppercase в коллекторе)
- Двойной st.rerun() убран
- Пробелы в тикере обрезаются
- Левое добавление тикеров при ошибке предотвращено

### Текущий статус
- 15 тикеров в сканере (7 вечных + 8 срочных)
- FutOI, свечи для всех
- HI2, FutOI_1h, FutOI_4h — ждут запуска агрегаторов

### Следующие шаги
- [ ] Расхождение Сводка vs Сканер
- [ ] Аудит вердикта — все ли блоки участвуют
- [ ] Запуск агрегаторов 1h/4h/HI2 для срочных
- [ ] Парная торговля — логарифмический спред
